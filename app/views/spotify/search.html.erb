<h2 class='playlist'>Random Playlist for <%= @artist_city.capitalize %></h2>

<ul class='list'>
  <% @songs.each do |song| %>
    <li>
      <a href="<%= song[:song] %>"><%= song[:artist] %></a>
    </li>
  <% end %>
</ul>

<div class='menu'>
  <a href='/' class="pure-button">Return To Home Page</a></br>
  <a href="/locations" class="pure-button">Return To Search Page</a>
</div>
